# ========================================
# Firebase Functions Environment Variables
# ========================================
# Copy this file to .env and fill in your values

# Firebase Admin SDK Authentication
# Note: For Firebase Functions, Application Default Credentials (ADC) are automatically used.
# You don't need to set any service account credentials in production.
#
# For local development only:
# 1. Download service account key from Firebase Console > Project Settings > Service Accounts
# 2. Set the path to the key file:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
#
# DO NOT include the service account JSON directly in environment variables!

# Storage Bucket
STORAGE_BUCKET=your-project-id.appspot.com

# Functions Region (optional, defaults to us-central1)
FUNCTIONS_REGION=asia-northeast1

# ========================================
# Optional: Runtime Configuration
# ========================================
# Set these using Firebase CLI:
# firebase functions:config:set somekey.somevalue="value"

# Example:
# firebase functions:config:set app.name="FerryTransit" app.env="production"