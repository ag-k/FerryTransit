# v2.5 リリースTODO

このファイルの必須チェックがすべて `[x]` になり、最終Go承認が記録されるまで v2.5 をリリースしない。

## リリース情報

- ステータス: 準備中
- 進捗: 8 / 70項目完了（11%、残り62項目）
- 対象: Web / iOS / Android
- リリース予定日: 未定
- リリース候補コミット SHA: 未定
- Git tag: 未定
- リリース責任者: 未定
- QA責任者: 未定
- リリースノート: 未作成

## 1. スコープと完了条件

- [ ] v2.5 に含める機能、修正、データ更新を確定した。
- [ ] v2.5のリリースQA・既知不具合・最終Go/No-Goを追跡するIssueを作成し、このTODOと相互参照した。
- [ ] ユーザー影響、互換性、データ移行、Firebase Rules / Functions / Storageへの影響をレビューした。
- [ ] 日本語・英語のリリースノートを作成した。

### v2.5 対象項目

#### はつみ交通「七類港―境港駅」連絡バスの追加

2026-07-31の調査では、はつみ交通株式会社が運行する「隠岐汽船連絡バス（七類・境港線）」がFerryTransitへ未収録だった。本番`data/bus-search`には七類港・境港の停留所はあるものの、両停留所を同一便で結ぶtripは0件で、登録済みの本土側接続バスは一畑バスの「松江駅―七類港」「松江駅―境港」のみだった。

完了時は、はつみ交通の公式時刻表、事業者名、片道運賃500円、隠岐汽船との接続条件を正しく収録し、Web・iOS・Androidの乗換検索で七類港―境港駅の直行便を日付に応じて検索・表示できること。

- [x] はつみ交通および隠岐広域連合の公式時刻表の出典URLを保存し、適用期間、全運行区分、フェリー・高速船との接続便、欠航時運休の注記を確定する。
- [x] はつみ交通用の交通ソース、GTFSフィード、agency、route、停留所、calendar、trip、stop_timesを追加し、七類港―境港駅間15分の全対象便を収録する。
- [x] 事業者名を「はつみ交通株式会社」、大人片道運賃を500円として定義し、一畑バスの事業者名・1,200円運賃と混同しないようフィードとクライアントの事業者・運賃解決処理を更新する。
- [x] 七類港と`HONDO_SHICHIRUI`、境港駅と`HONDO_SAKAIMINATO`の港・バス停接続を設定し、船→バス、バス→船、バス単独の検索で正しい乗換時間と乗降場所を表示する。
- [x] 日本語・英語の事業者名、路線名、運賃表示、運行上の注記、公式時刻表リンクを追加し、詳細モーダル、経路マップ、履歴、お気に入りでも一貫して表示する。
- [x] GTFS検証、compact bus-search JSON生成、停留所インデックス、公開manifest、Storage配信の各処理へ新フィードを組み込み、時刻表データの非同梱チェックを維持する。
- [x] 適用期間の境界日と代表日について、境港駅→七類港、七類港→境港駅、隠岐汽船接続を単体テストし、直行便が0件だった状態への回帰を防止する。
- [x] 一畑バスの松江駅―七類港・境港便、既存の船・バス横断検索、料金順を含む4種類の並び替えに退行がないことを自動テストで確認する。
- [ ] 本番静的成果物とWeb公開後の実データで七類港―境港駅の両方向、500円運賃、事業者名、公式リンク、0件・通信失敗・オフライン状態を確認する。
- [ ] iOS TestFlight版とGoogle Play内部テスト版を新しいアプリバージョンとして配布し、実機で七類港―境港駅の両方向検索、船との乗換、運賃、更新インストール後の表示を確認する。

#### JAL航空運賃の表示改善

2026-07-30の検証では、本番`data/timetable.json`にJAL便が10件（大阪（伊丹）線8件、出雲線2件）あり、全件で`price`が未設定だった。また、[`useRouteSearch.ts`](../../src/composables/useRouteSearch.ts)はJAL便の運賃を明示的に`0`としており、[`transit.vue`](../../src/pages/transit.vue)は`0`を「料金不明」と表示する。JAL便から空港連絡バスへ乗り継ぐ経路では、経路ヘッダーの`¥520`が全行程の合計に見える一方、実際には空港連絡バスだけの運賃である。

完了時は、航空運賃が未登録または変動することと、無料・徒歩・取得失敗を区別し、利用者が表示金額を全行程の確定総額と誤認しないこと。

- [ ] 航空運賃の表示仕様を確定する。最低限、`確定額あり`、`変動運賃・別途`、`取得不能`、`無料`、`徒歩`を区別する。
- [ ] JAL便の`price`が未設定の場合、区間運賃を「航空運賃は別途（変動）」等の誤解のない表現へ変更する。
- [ ] JAL便を含む経路のヘッダーと到着行を「¥520 + 航空運賃」等の表示にし、空港連絡バスだけの金額を全行程の確定総額として表示しない。
- [ ] JAL便の`price`が登録されている場合は、その金額を区間運賃と経路合計へ反映する。
- [ ] `0`を不明値として使う実装を廃止または正規化し、徒歩・無料・未登録運賃を型または状態値で区別する。
- [ ] 料金順ソートで、変動運賃を含む経路を既知部分の金額だけで「最安」と誤認させない仕様を決定して実装する。
- [ ] 経路マップ、検索履歴、お気に入り、共有URLなど、乗換検索結果を再表示する導線でも同じ運賃状態を維持する。
- [ ] 日本語・英語の文言を追加し、狭幅画面、200%拡大、ダークモードで文字切れや意味の欠落がないことを確認する。
- [ ] JAL公式の運賃確認ページへ案内する場合は、対象路線、外部リンク表示、WebViewからの遷移、リンク切れを確認する。

#### 航空運賃データと管理運用

- [ ] 本番・開発環境のJAL時刻表について、`price`の登録有無、意味（固定額・参考額・最安額等）、適用期間、更新責任者を定義する。
- [ ] 管理画面の時刻表編集、CSV/JSONインポート、公開JSON生成、Storage配信、クライアント正規化で`price`が欠落せず保持されることを確認する。
- [ ] `trip.price`を乗換検索の区間運賃計算へ渡し、値が有効な場合だけ採用する。
- [ ] JAL運賃を固定値として登録する場合は、運賃種別、予約時期、繁忙期等による変動を利用者へ明示し、更新期限と一次情報を記録する。
- [ ] 固定額を継続管理しない場合は、JAL時刻表の`price`を未設定のままとし、「別途（変動）」表示を正式仕様としてデータ検証へ追加する。
- [ ] 公開前に本番`data/timetable.json`のJAL全便を検査し、採用した運用方針と矛盾する`price`値がないことを確認する。

## 2. 実装・回帰テスト

- [ ] [`useRouteSearch.ts`](../../src/composables/useRouteSearch.ts)のJAL運賃`0`固定を新仕様へ置き換える。
- [ ] [`TransitSegment` / `TransitRoute`](../../src/types/index.ts)に、運賃状態や既知運賃合計を安全に表現できる型を追加する。
- [ ] [`transit.vue`](../../src/pages/transit.vue)の区間、経路ヘッダー、合計表示を新仕様へ変更する。
- [ ] [`RouteMapModal.vue`](../../src/components/map/RouteMapModal.vue)など、経路合計を表示する関連画面を新仕様へ統一する。
- [ ] JAL便単独で`price`未設定の場合に「変動運賃・別途」と表示される単体テストを追加する。
- [ ] JAL便＋空港連絡バスで「¥520 + 航空運賃」となり、`¥520`だけを総額表示しない単体テストを追加する。
- [ ] JAL便に`price`がある場合、航空区間と合計に正しい金額が表示される単体テストを追加する。
- [ ] 伊丹線・出雲線の両方向、JAL→バス、バス→JALを回帰テストする。
- [ ] 料金順、おすすめ順、出発時刻順、到着時刻順で変動運賃経路の扱いを回帰テストする。
- [ ] 徒歩区間の`-`、無料運賃、フェリー・内航船・固定運賃バスの既存表示に退行がないことを確認する。
- [ ] 日本語・英語、ライト・ダーク、モバイル・デスクトップの表示テストを追加または更新する。
- [ ] 既存のJAL運賃`0`と合計`¥520`を期待するテストを、新しい仕様に合わせて更新する。

## 3. 自動品質ゲート

- [ ] Node.js 22とリリース対象のnpmバージョンを記録し、クリーン環境で`npm ci`を成功させる。
- [ ] `npm run lint`を成功させる。
- [ ] `npm run test`を成功させる。
- [ ] `npm run test:e2e`を成功させる。
- [ ] `npm run build-prod`を成功させる。
- [ ] Functionsを変更した場合、`npm --prefix src/functions ci`と`npm --prefix src/functions run build`を成功させる。
- [ ] 新規警告、ブラウザコンソールエラー、未処理Promise rejectionがないことを確認する。

## 4. Web QA

- [ ] 本番静的成果物で「大阪（伊丹）空港→西郷」を検索し、JAL2331→隠岐空港連絡バスの乗換、待ち時間、運賃表示を確認する。
- [ ] 本番静的成果物で「出雲空港→西郷」と逆方向の「西郷→大阪（伊丹）空港 / 出雲空港」を確認する。
- [ ] 航空運賃未登録時に「料金不明」やバス運賃だけの総額が表示されず、新仕様の文言になることを確認する。
- [ ] 航空運賃登録時の検証データで、航空区間と経路合計が一致することを確認し、検証データを本番へ残さない。
- [ ] 最新Chrome、Safari、Firefox、EdgeとモバイルSafari / Chromeで表示・操作を確認する。
- [ ] 日本語・英語、ライト・ダーク・システム、200%拡大、キーボード操作、狭幅スマートフォンで確認する。
- [ ] 本番デプロイ後、公開URLでJAL乗換経路、公式リンク、実データ、コンソールエラーを再確認する。

## 5. iOS / Android QA

- [ ] `npm run cap:ios:build`とXcode Release Archiveを成功させ、TestFlight配布版を作成する。
- [ ] `npm run cap:android:build`と`./android/gradlew -p android bundleRelease`を成功させ、Google Play内部テスト版を作成する。
- [ ] iOS実機のTestFlight版で、伊丹 / 出雲―隠岐空港―西郷の運賃表示と外部リンクを確認する。
- [ ] Android実機の内部テスト版で、伊丹 / 出雲―隠岐空港―西郷の運賃表示と外部リンクを確認する。
- [ ] オンライン、完全オフライン、通信復帰、バックグラウンド復帰、日付変更後も運賃状態が誤って確定額へ変わらないことを確認する。
- [ ] アプリ更新後も設定・お気に入り・履歴が保持され、新しい運賃表示で再検索できることを確認する。

## 6. QA記録・既知不具合

- [ ] QA記録にコミットSHA、バージョン / ビルド番号、日時、担当者、環境、端末、ブラウザ、確認経路、JAL便名、運賃データ状態を記録する。
- [ ] 既知不具合を重要度、影響、回避策、延期先、承認者とともに記録する。
- [ ] 重大・高優先度の未解決不具合が0件であることを確認する。

| ID | 重要度 | 影響 | 回避策 / 延期先 | 承認者 |
| --- | --- | --- | --- | --- |
| 未登録 | - | - | - | - |

## 7. リリース実行・最終判定

- [ ] `package.json`、`.env.production`、iOS、Androidのバージョンとビルド番号をv2.5で揃える。
- [ ] リリース対象コミットSHAを固定し、Web・iOS・Androidで同じソースを使用した。
- [ ] Web公開、App Store提出、Google Play提出の実行日時と結果を記録する。
- [ ] 全必須TODOが完了し、証跡へのリンクがあることを確認する。
- [ ] QA責任者のGo承認を記録する。
- [ ] リリース責任者のGo承認を記録する。

### 最終判定

- 判定: 未判定
- QA承認者 / 日時: 未定
- リリース承認者 / 日時: 未定
- 備考: 未記入
